/** Describes system-dependent settings. */
interface ISystemDependentSettings {

    /** The name or full path of the shell to use in the integrated terminal. */
    shell: string;
}

export default ISystemDependentSettings;
