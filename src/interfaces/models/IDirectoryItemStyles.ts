/** Describes the styles of the DirectoryItem component's sub-parts. */
interface IDirectoryItemStyles {
    /** The background style of the directory item. */
    backgroundStyle: React.CSSProperties;
    /** The style of the 'name' column. */
    nameColumnStyle: React.CSSProperties;
    /** The style of the 'size' column. */
    sizeColumnStyle: React.CSSProperties;
    /** The style of the 'modified on' column. */
    lastModifiedColumnStyle: React.CSSProperties;
}

export default IDirectoryItemStyles;
