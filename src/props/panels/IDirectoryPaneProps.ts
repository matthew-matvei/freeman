import { IDirectoryCommonProps } from "props/panels";

/**
 * Describes the properties of a directory pane component.
 */
interface IDirectoryPaneProps extends IDirectoryCommonProps { }

export default IDirectoryPaneProps;
